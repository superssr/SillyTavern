# SillyTavern 数据库持久化部署指南

## 概述
完整的PostgreSQL数据库持久化系统已经配置完成，包括：
- 用户数据、API密钥、角色卡、世界书、聊天记录等所有数据的数据库存储
- 透明的数据库集成，优先使用数据库存储，回退到文件系统
- 自动数据库表初始化和用户创建

## 已完成的配置

### 1. 数据库管理器 (`src/database-manager.js`)
- 完整的PostgreSQL持久化解决方案
- 支持的数据类型：
  - 用户账户（密码、权限、设置）
  - API密钥（OpenAI、DeepSeek、Anthropic等）
  - 角色卡（完整角色数据）
  - 世界书（条目和描述）
  - 聊天记录（消息历史）
  - 用户设置（个性化配置）
  - 预设（各类模板）
  - 背景图片

### 2. 数据库集成层 (`src/database-integration.js`)
- `UserStorageProxy` - 用户数据透明代理
- `ApiKeyStorageProxy` - API密钥存储代理  
- `CharacterStorageProxy` - 角色卡存储代理
- `SettingsStorageProxy` - 设置存储代理

### 3. 服务器集成 (`src/server-main.js`)
- 在用户存储初始化后自动初始化数据库
- 服务器启动流程：
  ```
  initUserStorage → initDatabaseIntegration → 其他初始化步骤
  ```

### 4. 用户模块修改 (`src/users.js`)
- 所有用户数据读写操作现在使用数据库代理
- 保持完全向后兼容性
- 支持Mike用户的自动创建（环境变量控制）

## 环境变量配置

### 必需的环境变量

```bash
# PostgreSQL数据库连接
DATABASE_URL=postgresql://sillytavern_db_user:yDepy0PpspqhgbXYuDbeRobSPXXOEp2P@dpg-d2q9mjv5r7bs73afgkeg-a/sillytavern_db

# 自动创建Mike用户（用户名：mike，密码：mike）
CREATE_USER_MIKE=true
```

### 可选的环境变量

```bash
# 生产环境配置
NODE_ENV=production
LISTEN=true
PORT=10000

# 其他API配置
OPENAI_API_KEY=your-openai-key
DEEPSEEK_API_KEY=your-deepseek-key
ANTHROPIC_API_KEY=your-anthropic-key
```

## 数据库结构

自动创建的表：
- `users` - 用户账户信息
- `api_keys` - 各服务API密钥
- `characters` - 角色卡数据
- `world_books` - 世界书内容
- `chats` - 聊天记录
- `settings` - 用户设置
- `presets` - 预设模板
- `backgrounds` - 背景图片

## 部署步骤

### 1. 在 Render.com 上：

1. **设置环境变量**：
   ```
   DATABASE_URL=postgresql://sillytavern_db_user:yDepy0PpspqhgbXYuDbeRobSPXXOEp2P@dpg-d2q9mjv5r7bs73afgkeg-a/sillytavern_db
   CREATE_USER_MIKE=true
   NODE_ENV=production
   ```

2. **重新部署服务**：
   - 系统会自动初始化数据库表
   - 自动创建默认用户（mike和default-user）

### 2. 验证部署：

1. **检查数据库连接**：
   - 服务器启动日志应显示："Database connected successfully"
   - 如果失败，会显示："No database URL found, using file-based storage"

2. **测试用户登录**：
   - 用户名：`mike`
   - 密码：`mike`
   - 应该能够成功登录并保持会话

3. **验证数据持久性**：
   - 创建角色卡或修改设置
   - 重启容器后数据应该保持

## 故障排除

### 数据库连接问题：
- 检查DATABASE_URL格式是否正确
- 确认PostgreSQL实例已启动且可访问
- 查看服务器日志中的具体错误信息

### 用户无法登录：
- 检查CREATE_USER_MIKE环境变量是否设置
- 查看日志是否显示"Created default user: mike"
- 如果数据库连接失败，系统会回退到文件存储

### 数据不持久：
- 确认数据库连接成功（查看启动日志）
- 检查数据库表是否正确创建
- 验证用户操作是否触发数据库写入

## 数据迁移

如果需要从文件存储迁移到数据库：
1. 部署新版本（带数据库支持）
2. 用户数据会在首次访问时自动从文件同步到数据库
3. 后续操作优先使用数据库

## 监控和维护

- 数据库连接状态会在服务器启动时记录
- 所有数据库操作错误会记录到控制台
- 支持优雅的降级：数据库不可用时自动使用文件存储

---

**配置完成时间**：${new Date().toISOString()}
**Mike用户**：用户名 `mike`，密码 `mike`，管理员权限